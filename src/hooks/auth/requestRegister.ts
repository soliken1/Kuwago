//The request needed for registering, here return the registerData and define the route, prepare the payload and
//return 4 parameters. (Same structure for login instead the register expects 6 required fields for the payload)

"use client";

// import { useState } from "react";
// import axios, { AxiosError } from "axios";

// interface RegisterPayload {
//   firstName: string;
//   lastName: string;
//   email: string;
//   username: string;
//   phoneNumber: string;
//   password: string;
// }

// interface RegisterResponse {
//   // Update this based on actual API response structure
//   token?: string;
//   message?: string;
//   [key: string]: any;
// }
